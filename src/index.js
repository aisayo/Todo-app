// the entry into react app || root file

//imports & libraries

import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

//what am i doing??
//render our react app to the dom

ReactDOM.render(<App />, document.getElementById("root"));
